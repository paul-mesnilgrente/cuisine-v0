{% extends '::base.html.twig' %}

{% block title %}{{ action }} un produit ménager{% endblock %}

{% block body %}
    <h1>{{ action }} un produit ménager</h1>
    {{ form_start(form) }}
      {{ form_rest(form) }}
      <input type="submit" value="Valider" class="btn btn-primary"/>
    {{ form_end(form) }}
{% endblock %}
